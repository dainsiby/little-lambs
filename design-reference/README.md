# Approved design reference

`astra-approved-landing-page.png` is the user-supplied visual reference. It is stored outside `public` and is not imported by the application. No screenshot navigation, buttons or text were cropped for use in the page.

The interface is implemented with React, HTML, Tailwind/CSS and SVG divider paths. The illustration is a separate continuous asset, described in `../ASSETS.md`.
